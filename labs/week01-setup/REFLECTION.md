# Week 1 Reflection

The trickiest part of today's setup was getting Python 3.11 specifically, since my Mac already had Anaconda's Python 3.13 as the default and my first venv was silently created with the wrong version, plus my existing SSH key was not yet linked to GitHub so the first `ssh -T` failed with "Permission denied (publickey)".
